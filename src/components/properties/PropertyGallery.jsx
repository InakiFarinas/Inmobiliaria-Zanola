import React from "react";

export default function PropertyGallery({ images = [] }) {
	return (
		<div className="property-gallery">
			{/* gallery implementation preserved in original file */}
		</div>
	);
}
